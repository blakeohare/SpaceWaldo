class AbstractScene {
	
	field _next = null;
	
	constructor() {
		
	}
	
	function switchScene(scene) {
		this._next = scene;
		scene._next = null;
	}
	
	function update(inputManager) {
		throw new Exception("Override me!");
	}
	
	function render(rc) {
		throw new Exception("Override me!");
	}
}
