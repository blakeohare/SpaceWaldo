class OptionsScene : AbstractScene {
	constructor() : base() {
	
	}
	
	function update(input) {
		if (input.pressedThisFrame) {
			this.switchScene(new TitleScene());
		}
	}
	
	function render(rc) {
		ImageLibrary.get('manual.png').draw(0, 0);
	}
}
